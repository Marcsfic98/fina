import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width:"80%",
    height:300,
    borderRadius:16,
    overflow:"hidden",
    alignItems:"center",
    marginBottom:30
  }
});