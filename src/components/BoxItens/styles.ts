import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width:"80%",
    height:"100%",
    borderRadius:16,
    overflow:"hidden",
    alignItems:"center",
    marginBottom:90
  },
  title:{
    color:"#fff",
    marginTop:20,
    fontSize:20
  }
});